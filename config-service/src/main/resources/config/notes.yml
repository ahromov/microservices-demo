server:
  port: 8082
spring:
  datasource:
    url: jdbc:h2:./data/notes/sampledata;OLD_INFORMATION_SCHEMA=TRUE
    username: sa
    password: sa
eureka:
  client:
    eureka-service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true